name: CI Pipeline

on:
  push:
    branches:
      - main

jobs:
  update:
    name: update thing
    runs-on: ubuntu-latest

    steps:
      - name: Checkout code
        uses: actions/checkout@v4

      - name: Run update step
        run: echo "update thing"
